{
  "name": "zapmyco-docs",
  "private": true,
  "scripts": {
    "dev": "rm -rf .cache && openmanual dev",
    "build": "rm -rf .cache && rm -rf dist && openmanual build",
    "start": "npx -y serve dist -p 3000"
  },
  "dependencies": {
    "openmanual": "^0.17.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^22.0.0"
  },
  "packageManager": "pnpm@10.30.3",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp"]
  }
}
